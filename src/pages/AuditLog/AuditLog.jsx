import React from 'react';
import styled from 'styled-components';

const PageContainer = styled.div``;

const AuditLog = () => {
  return (
    <PageContainer>
      <h1>Audit Log</h1>
      {/* Table or list to display audit trail from Firestore will go here */}
    </PageContainer>
  );
};

export default AuditLog;